# PROJECT_BRIEF

Status: Canonical | Descriptive

Project: Lumis-1 repository rehabilitation for manual, evidence-backed operator execution

## Canonical Execution Path

- `configs/*.yaml`
- `THE NOTEBOOK-sanity.ipynb` and `THE NOTEBOOK-updated.ipynb` as the active standalone Colab surfaces
- runtime helpers in `lumis1/*`
- tests in `tests/*`
- current evidence in `workspace/reports/*`
- proof-bearing identity artifact in `Dataset/identity_dataset/output/full_run_codex_spark_xhigh/`

## Status Taxonomy

- `Canonical`: the active path contributors should follow now
- `Proof-bearing`: backed by direct repository evidence
- `Descriptive`: intended behavior or current interpretation, not proof by itself
- `Draft`: planned or incomplete
- `Historical`: preserved for traceability, not part of the active execution path

## Proven Repository Truth

- The strongest completed artifact is the canonical identity run `identity-20260303T162023Z`.
- The identity artifact is validated by `workspace/reports/identity_validation.json`.
- The current open/full dataset path only proves sample-scale outputs under `workspace/interim/*` and `workspace/final/*`.
- `workspace/runs/` does not currently prove completed SFT, DPO, evaluation, or export.
- The archive is historical only and is not part of the authoritative execution path.

## Scope

- Maintain an evidence-backed notebook/config/runtime path.
- Keep scripts only when they are thin parity wrappers over the canonical runtime helpers.
- Make future training/eval/export claims impossible without `workspace/runs/<run_id>/` evidence.

## Hard Constraints

- No remote execution claims without proof-bearing run artifacts.
- No automatic maturity claims from notebook presence or sample outputs.
- Identity artifact remains read-only and is the regression anchor.
- Unsloth + Qwen3.5 thinking-off policy remains enforced for the active dataset path.
- Notebook 91 defaults to an Unsloth-first install path on Colab; repo-pinned dependency sync is no longer the default Colab bootstrap strategy.
- Qwen3.5 non-thinking execution must be probed through the tokenizer chat template with `enable_thinking=False`; `/nothink`-style assumptions are not treated as sufficient proof.
- Notebook 91 embeds its own runtime/config surface, so the operator does not need to attach repo YAML files separately.
- Notebook 91 follows an Unsloth-first install contract, records the chosen install path and versions under `workspace/reports/bootstrap/`, and only installs supplemental packages after the Unsloth core stack is in place.
- Notebook 91 now ships as two explicit surfaces: `THE NOTEBOOK-sanity.ipynb` for bounded proving runs and `THE NOTEBOOK-updated.ipynb` for the full configured run, while still auto-selecting a higher-throughput G4 profile on RTX PRO 6000 Blackwell / ~96GB-class runtimes.
- Notebook 91 recovers Colab Drive mounting when `/content/drive` is already non-empty, auto-downloads missing identity files from `STnoui/lumis1-identity`, builds canonical merged datasets, and auto-downloads the final artifact or `final_deliverables.zip` to the local browser session.
- Notebook 91 materializes surrogate local image assets for placeholder-only identity rows, writes multimodal blocks with public-compatible `image` / `path` / `image_path` keys for supported HF multimodal sources, and treats multimodal SFT as the primary training path.
- Notebook 91 exports GGUF first from the strongest completed artifact of the same run and records a DPO skip reason when the multimodal path only has text preferences.
- Concrete multimodal SFT remains unproven until a real Colab run succeeds end to end; identity multimodal supervision still depends on surrogate screenshot/document images, not original curated screenshots.
- GGUF export can now complete unattended in `structural_only` mode when required variants exist but parity-pair smoke inputs are absent; this is not the same as proof-bearing parity verification.

## Runtime Outputs

- `workspace/reports/`: current validation and descriptive reports
- `workspace/final/`: current sample-scale dataset artifacts unless regenerated by a future proof-bearing run
- `workspace/runs/`: reserved for real proof-bearing SFT/DPO/eval/export evidence

## Repository Memory

- `PROJECT_CHANGELOG_DETAILED.md` is append-only and records each meaningful work block.
- `PROJECT_TIME_CAPSULE.md` records the current best understanding for the next agent without requiring a full reread.
